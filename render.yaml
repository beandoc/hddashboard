services:
  - type: web
    name: hd-dashboard-pro
    env: python
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python scripts/pre_deploy.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
